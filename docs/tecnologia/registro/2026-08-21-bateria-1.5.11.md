# Bateria 1.5.11 na maquina — primeira rodada (2026-08-21)

> **Executor:** Marcos (acoes) + @Tony (verificacao) | **Maquina:** DESKTOP-VMSM6LE
> Update automatico 1.5.10 -> 1.5.11 as 11:45. Pacote publicado pelo Marcos.

---

## Linha do tempo

| Hora | O que |
|---|---|
| 11:42:37 | boot |
| 11:44:56 | Service sobe (delayed autostart, 2min19s — configuracao, nao defeito) |
| 11:44:58 | worker 1.5.10 sobe, **acusa SESSAO_INTERROMPIDA** (`NoCleanShutdownFlag`) |
| 11:45:00 | update 1.5.11 encontrado e baixado |
| 11:45:39 | SHUTDOWN para update; worker emite **um** LOGOUT e grava a flag de shutdown limpo |
| 11:45:56 | worker 1.5.11 sobe |
| 11:45:57 | **SESSAO_INTERROMPIDA suprimida** (flag presente) e **LOGIN suprimido** (mesmo LogonId) |
| 11:46:02 | 1.5.11 instalada |

---

## O que passou

**Update automatico (itens e1–e7).** Detectou, baixou, aplicou e voltou sozinho em ~23s. Sem
intervencao.

**A-61 / A-62 na pratica.** O ciclo do update e um logoff-e-login em miniatura, e o desenho novo
se comportou como prometido: o worker se despediu, gravou a flag, e o worker seguinte **calou os
dois alarmes falsos** — interrupcao e login repetido. Um LOGOUT no log, nunca dois.

O alarme das 11:44:58 e **legitimo**: o reboot saiu da 1.5.10, que ainda nao gravava a flag.
Nao conta como falha desta rodada.

**A-58 vivo.** `Status inicial do colaborador: ATIVO (silencio de 116s). Nenhuma transicao
emitida.` — adota sem emitir, como decidido em D4.

**A-60 renomeou o buffer.** `autonomous-buffer.db` virou `autonomous-buffer-S1.db`. A varredura
achou o arquivo da versao anterior e recolheu o evento que estava dentro.

**Minha correcao de D6 esta valendo.** `clean-shutdown-1.json` existe e foi lido pelo worker
seguinte. Sem a atribuicao no construtor, esse arquivo nunca teria sido escrito.

---

## A-63 — evento recolhido do buffer antigo e rejeitado para sempre

**Severidade: MEDIA-ALTA.** Atinge os itens **11** e **14**, os dois que o @Bucky mandou olhar
primeiro.

### O que acontece

A varredura recolheu o evento, mas **sem dono**:

```
11:45:56 [WRN] Buffer orfao recolhido. Sessao=0, Usuario=(desconhecido), Eventos=1.
```

E o backend recusa:

```
11:46:59 [WRN] Batch upload failed. Status=400, User=null, Attempt=1
  {"codigo":"PAYLOAD_INVALIDO",
   "issues":[{"path":"windowsUsername","code":"invalid_type",
              "message":"Invalid input: expected string, received null"}]}
```

**E fica em laco.** Uma tentativa por minuto, indefinidamente — 11:46:59, 11:48:03, 11:49:08.
Sempre `Attempt=1`: o contador nao avanca, cada ciclo trata como lote novo. Nao ha teto, nao ha
descarte, nao ha fila morta.

### Por que o dono se perdeu

O desenho previa "entregar sob o nome do dono original". Funciona para
`autonomous-buffer-S<N>.db`, porque o numero da sessao esta no nome. **O arquivo da versao
anterior se chama `autonomous-buffer.db` — sem sessao e sem usuario.** A varredura cai no
fallback `Sessao=0, Usuario=null` e monta um lote que o backend nunca vai aceitar.

Ou seja: o caso que a varredura foi escrita para cobrir e justamente o unico em que ela nao tem
a informacao de que precisa.

### Impacto

- **Item 14 passa pela metade:** o buffer antigo foi recolhido, mas o evento **nao chega ao
  banco**. E o unico item da lista que perde dado.
- **Item 11 reprova:** ha lote rejeitado, e permanente.
- Um evento envenenado por maquina, a cada atualizacao, na frota inteira.
- Aviso a cada minuto no log de todas as maquinas.
- O buffer nunca encolhe. Casa com a divida #1 de hoje — as funcoes de limpeza do buffer no
  Windows sao conchas vazias.

**Nao bloqueia os demais lotes.** Os lotes com dono seguiram normalmente no mesmo ciclo.

### Caminhos (decidir com o @Bucky)

1. **Atribuir ao dono provavel.** No update, a sessao ativa e conhecida — o recolhimento
   acontece com o Service ja de pe e com sessao listada. Carimbar o `windowsUsername` dessa
   sessao quando houver exatamente uma.
2. **Ler o dono de dentro do banco**, se o evento gravado ja carregar o usuario.
3. **Teto de tentativas + fila morta.** Necessario de qualquer jeito: hoje qualquer evento que o
   backend recuse fica em laco eterno. Isto e maior que o A-63.

O caminho 3 vale sozinho, independente do 1 e do 2.

### Aceite

- Atualizar da versao anterior nao deixa nenhum lote em laco.
- O evento recolhido chega ao banco com `windows_username` preenchido.
- Evento que o backend recuse por conteudo sai da fila depois de N tentativas, com registro.

---

## Ainda por fazer nesta bateria

Item 1 (reboot) precisa ser refeito **saindo da 1.5.11** — o desta rodada saiu da 1.5.10 e por
isso o alarme apareceu. Os itens 2 a 13 e 15 nao foram executados.

---

# Rodada automatizada — 12:37 as 12:52

## Item 2 — matar o worker a forca: **PASSA**

Worker 9368 morto as 12:37:29.

| Hora | O que |
|---|---|
| 12:37:29 | pipe cai (EOF); **evento de janela aberto e fechado** com motivo `WorkerDied` |
| 12:37:34 | watchdog confirma a morte (5s — a janela de confirmacao do A-61) |
| 12:37:36 | worker novo de pe, PID 3304 |
| 12:37:37 | **SESSAO_INTERROMPIDA e LOGIN suprimidos** |

Sete segundos ponta a ponta. Nao ficou evento aberto e nao houve alarme falso.

## Item 8 — PAUSA / AUSENTE / ATIVO: **PASSA**

Medido no proprio uso da maquina, sem roteiro:

```
12:01:50  PAUSA   -> AUSENTE   (silencio de 919s = 15min19s)
12:34:08  AUSENTE -> ATIVO     (silencio de 0s — mexeu, voltou na hora)
12:50:03  ATIVO   -> PAUSA     (silencio de 322s = 5min22s)
```

Bate com a regra do Android nos tres sentidos: 5 minutos para pausa, 15 para ausente, e
qualquer interacao devolve para ativo imediatamente. **O A-58 esta fechado na pratica.**

## Cenarios automatizados: **7 de 7**

| | | |
|---|---|---|
| N4 config de captura efetiva | PASSOU | 1s |
| N6 InstalacaoId estavel (A-49) | PASSOU | 20,2s |
| N1 recusa a versao ja instalada (A-46) | PASSOU | 16,1s |
| N2 cooldown por relogio de parede (A-41) | PASSOU | 27,2s |
| N7 device JWT expirado renova | PASSOU | 27,2s |
| N8 403 + `X-Agent-Revoked` (ADR-001) | PASSOU | 71,6s |
| N9 vinculo completo / sem vinculo nao captura | PASSOU | 1s |

N9 confirmou na maquina real: token DPAPI descriptografa, vinculo de 20/08, sem marca de
revogacao, colaborador 3336. **Esta maquina continua capturando com a regra nova.**

## Testes de codigo: **935 verdes**

Quatro suites, zero falhas, com o bump 1.5.11 e a correcao de D6 dentro.

## Falso alarme que eu quase reportei

A flag de shutdown limpo de 11:45:39 esta **sem dono** (`logonId` nulo), e a primeira leitura foi
de que isso desligaria a deteccao de queda para sempre.

**Nao e defeito.** Aquela flag foi escrita pelo worker **1.5.10**, que nao tinha o conceito. O
`SessaoInterrompidaDecider` trata flag sem dono como legado de proposito, para nao inventar
alarme de queda em maquina que desligou direito durante a atualizacao da frota.

Conferido com sonda direta: `QueryLogonId` responde normal para o usuario interativo nesta
maquina (`1:134317970010188694:NoisyTech`). A API funciona.

**Fica um ponto a verificar**, que so um desligamento limpo sob a 1.5.11 prova: se a flag nova
sai com o dono preenchido. Depende do item 5 ou 6, que precisam do Marcos.

---

## Regressivo local: **20 passou, 0 falhou, 1 atencao**

O unico ponto de atencao e o **A-55**, ja conhecido e ja aceito pelo Marcos: 2 eventos com o
caminho da URL dentro do **titulo da janela**. O campo de dominio esta certo; a superficie do
titulo nao. Decisao de produto, igual ao A-47.

**Divida de documentacao encontrada de passagem:** o proprio regressivo ainda descreve o M7 como
"desde a v1.5.0 quem deriva o status e o backend". Isso deixou de valer hoje — o A-58 devolveu o
calculo ao Agent. E o mesmo comentario falso que sustentou o engano por tres meses
(item 4 da divida de D4). Corrigir o texto do script.

---

# A-64 — nada no Agent desiste nunca

**Severidade: MEDIA.** Mesmo defeito de fundo do A-63, em outro lugar. Vale tratar junto.

## Como apareceu

Item 15 da lista (usuario removido, buffer sai do disco sozinho). Plantei um buffer
`autonomous-buffer-S77.db` de uma sessao que nao existe, carimbado ha 40 min, e o arquivo estava
corrompido — sem as tabelas dentro.

A varredura **achou** corretamente. E entao:

```
13:05:10 [WRN] Buffer orfao autonomous-buffer-S77.db nao pode ser lido. Ficara para a proxima varredura.
13:05:25 [WRN] (identico)
13:05:40 [WRN] (identico)
13:05:55 [WRN] (identico)
```

**A cada 15 segundos, para sempre.** 4 avisos por minuto, 5.760 por dia, ate alguem apagar o
arquivo a mao. Parou no segundo em que removi o arquivo — confirmado.

## O padrao

`OrphanBufferSweeper.RecolherAsync`: falha na leitura loga aviso e "fica para a proxima
varredura". Nao ha contador, nao ha quarentena, nao ha teto.

A intencao esta certa e esta escrita no proprio codigo — *"so apaga depois de a entrega ter dado
certo; perder o arquivo com evento dentro seria trocar vazamento de disco por perda de dado"*.
O que falta e o caso em que a entrega **nunca** vai dar certo.

**E o mesmo buraco do A-63**, que retenta um lote recusado a cada 60s indefinidamente — 67
tentativas ate agora, todas com `Attempt=1`.

Duas ocorrencias, uma causa: **o Agent nao tem o conceito de desistir.** Qualquer coisa que
falhe de forma permanente vira laco eterno e silencioso.

## Aceite

- Arquivo ilegivel vai para quarentena (renomear com sufixo) depois de N tentativas, com **um**
  registro, e para de ser varrido.
- Lote recusado por conteudo sai da fila depois de N tentativas, com registro.
- Nenhum aviso identico se repete indefinidamente no log.

## Ressalva honesta

O arquivo corrompido foi criado por mim, nao apareceu sozinho. O **comportamento** de retentar
sem fim e real e independe de como o arquivo ficou ilegivel — disco cheio, queda de energia no
meio da escrita, truncamento no update.

**Sobra na maquina:** `autonomous-buffer-S77.db-shm` e `-wal`, presos pelo Service. Somem no
proximo restart do servico. Nao atrapalham.

---

# Placar da rodada automatizada

| Bloco | Resultado |
|---|---|
| Testes de codigo | **935 passou, 0 falhou** |
| Cenarios N1–N9 (7 executaveis) | **7 passou, 0 falhou** |
| Regressivo local | **20 passou, 0 falhou, 1 atencao (A-55, ja aceito)** |
| Item 2 — matar o worker | **passa** |
| Item 8 — PAUSA/AUSENTE/ATIVO | **passa** |
| Item 11 — nenhum lote rejeitado | **reprova (A-63)** |
| Item 14 — buffer recolhido no update | **passa pela metade (A-63)** |
| Item 15 — buffer de usuario removido | **reprova (A-64)** |

## Nao rodado, e por que

**N3 e N5** aplicam um **update real** e deixam a maquina em outra versao — o proprio runner
avisa que e preciso reinstalar depois. Tirariam a maquina da 1.5.11 no meio da bateria e
inviabilizariam os itens 5, 6, 7, 12 e 13. **Precisa do aval do Marcos.**

O N5 (`buffer sobrevive ao update`) e justamente o que cobre o A-63 — vale rodar depois que a
correcao existir, nao agora.

**Itens 1, 3, 4, 5, 6, 7, 9, 10, 12, 13** dependem de acao fisica do Marcos: reboot, dormir,
logoff, desligar, segunda conta, bloquear a tela, uma noite parada.

**Camada de banco** (`verificar-eventos-base.js`): a janela do regressivo ficou salva em
`ultimo-regressivo.json` (15:56:41Z a 16:02:24Z). Falta a credencial do banco nesta sessao — e a
senha antiga precisa ser trocada de qualquer forma.

---

# Testes guiados com o Marcos — tarde de 21/08

## Item 9 — bloquear e desbloquear: **PASSA**

Bloqueio 13:55:12, desbloqueio 13:56:00. Janela e ociosidade fechadas no bloqueio, monitores
suspensos e retomados. **Nenhuma transicao de status** — que e o criterio. Os dois eventos
subiram em segundos.

## Item 3 — dormir 3 minutos: **PASSA**

```
13:58:21  LOCK (o Windows bloqueia antes de dormir)
13:58:22  PowerMode Suspend — "sessao ja bloqueada, nada a fazer" (sem evento duplicado)
14:01:52  PowerMode Resume
14:01:57  Gap de wall-clock de 216s -> IdleEvent sintetico
```

O sono virou **ociosidade de 216s**, nao tempo trabalhado. O worker sobreviveu, nao foi
relancado, nada se perdeu.

### Dois pontos menores

**1. Mensagem mentirosa.** O aviso diz *"Gap de wall-clock detectado **sem PowerMode event**"* —
mas o Suspend e o Resume foram detectados e logados 4 segundos antes. O resultado esta certo, o
texto esta errado. Mesma familia do comentario falso do `DailyBoundaryWorker` (divida 4 de D4):
log errado e o que faz alguem acreditar na coisa errada por meses.

**2. Cinco minutos e meio sem enviar depois de acordar.** O lote preparado as 13:58:22, no
instante do sono, ficou pendurado ate 14:03:30 — a conexao aberta antes de dormir nao morre
sozinha. Falhou, foi refeito, e os 13 eventos subiram inteiros as 14:03:35. **Nada se perdeu**,
mas numa maquina que dorme muito isso atrasa os dados a cada ciclo. O timeout do cliente HTTP
nao esta cobrindo o caso.

## Itens 7 e 12 — dois usuarios: **PASSAM**

Troca rapida NoisyTech -> Marcos -> NoisyTech, com uso real na segunda conta.

- **Dois workers vivos**, um por sessao (PID 6452 sessao 1, PID 16276 sessao 2).
- **Dois buffers separados**, `autonomous-buffer-S1.db` e `-S2.db`, ambos gravando.
- **Atribuicao correta na origem:** lotes saem carimbados `User=NoisyTech` e `User=Marcos`,
  intercalados no mesmo minuto, sem mistura.
- **Zero falha de escrita** na sessao 2 sob a 1.5.11.
- **LGPD respeitada:** o Chrome no YouTube gravou `UrlDominio: youtube.com`, sem caminho.

O `SESSAO_INTERROMPIDA` da sessao 2 as 14:06 e **legitimo** — a sessao do Marcos foi derrubada
pelo reboot das 11:42 sem despedida, porque a 1.5.10 nao gravava a flag. Sob a 1.5.11 nao deve
mais acontecer; o item 6 prova.

### Correcao a um ponto de D4

O @Bucky registrou, ao justificar o desenho do A-60, que *"por isso `session-state-N` sempre
funcionou"*.

**Nao funcionava.** O log da sessao 2 tem **52 falhas** de `Falha ao persistir session-state.
Access to the path 'session-state-2.json' is denied`, so no dia de hoje, das 00:00:40 as
11:42:10 — todas sob a 1.5.10.

**A ultima e as 11:42:10. Depois do update, nenhuma.** A 1.5.11 corrigiu junto, sem que esse
caso estivesse no escopo.

Nao muda o desenho aprovado, que segue certo. Muda a premissa: o `session-state-N` sofria do
mesmo problema de dono de arquivo que o buffer, e ninguem tinha visto porque o erro so aparece
no log da segunda sessao, que ninguem abre.

## Item 13 — Service derrubado com dois usuarios logados: **PASSA**

Service parado as 14:11:55, de volta as 14:12:16.

**Cada sessao cuidou da sua:**

```
14:11:49  S1  SHUTDOWN -> LOGOUT proprio (SERVICE_STOP) -> flag gravada
14:11:49  S2  SHUTDOWN -> LOGOUT proprio (SERVICE_STOP) -> flag gravada
14:12:17  S1  SESSAO_INTERROMPIDA e LOGIN suprimidos
14:12:17  S2  SESSAO_INTERROMPIDA e LOGIN suprimidos
14:12:19  3 eventos do NoisyTech subiram
14:12:20  2 eventos do Marcos subiram
```

Um LOGOUT por sessao, nunca dois. Nenhum alarme falso. Nenhuma falha de escrita.

### A duvida em aberto esta respondida

Este foi o primeiro desligamento limpo sob a 1.5.11, e as flags sairam **com dono**:

```json
clean-shutdown-1.json  { "sessionId": 1, "logonId": "1:134317970010188694:NoisyTech", ... }
clean-shutdown-2.json  { "sessionId": 2, "logonId": "2:134318056055988866:Marcos",    ... }
```

Confirma que o desenho de D5 esta inteiro: a flag vale so para o logon que a gerou, ninguem
precisa apaga-la, e flag de outro logon e ignorada. A flag sem dono de 11:45 era mesmo heranca
da 1.5.10, como eu tinha concluido — **nao havia defeito ali**.

---

# Correcao ao A-63 — como o laco terminou de verdade

Eu registrei que o lote envenenado retentava "indefinidamente". **Termina** — e o jeito como
termina e pior do que o laco.

## O que realmente aconteceu

```
11:46:59  primeira recusa do backend real (staging-api-events)
   ...    67 recusas, uma por minuto, durante 57 minutos
12:43:17  ultima recusa do backend real
12:43:22  ACEITO por  http://localhost:18081/api/agent/events
```

O `localhost:18081` e o **stub dos cenarios N7/N8/N9**, que estava de pe naquele momento porque
eu estava rodando a bateria automatizada. O stub aceita qualquer coisa.

## O que isso significa

1. **O A-63 continua real e mais grave.** Numa maquina de cliente nao existe stub. As 67
   tentativas em 57 minutos teriam continuado sem fim.
2. **O evento se perdeu.** Nao foi corrigido nem descartado: foi entregue a um servidor falso e
   apagado da fila. Nunca chegou ao banco.
3. O contador `Attempt=1` em todas as 67 confirma que nao ha retentativa com controle — cada
   ciclo trata como lote novo.

## Efeito colateral do proprio harness, a registrar

Rodar N7/N8/N9 numa maquina com **dados reais pendentes** faz o stub engolir eventos de verdade
que ainda nao subiram. Nao e defeito do produto, e da forma de testar.

**Sugestao:** o stub deve recusar (ou separar num arquivo a parte) lote cujo `agenteId` nao seja
o sintetico do cenario, e o runner deve avisar quantos lotes reais interceptou.

## Ajuste no aceite do A-63

Alem do que ja estava escrito:

- Lote recusado por conteudo **nunca** pode sair da fila por ter sido aceito em outro endereco.
  O destino tem de fazer parte da identidade do lote.

## Item 4 — dormir 31 minutos: **PASSA**

Dormiu 14:18:01, acordou 14:49:10. **31 minutos.**

| O que | Resultado |
|---|---|
| Worker sobreviveu | sim — nao morreu, nao foi relancado |
| Service acusou worker morto | **nao, nenhuma vez** |
| Tempo dormindo | IdleEvent sintetico de **1876s** |
| Status | ATIVO -> AUSENTE (silencio de 1869s) |
| Sessao 2 (Marcos) | intacta, eventos preservados e enviados |

**Este e o teste que mata o A-56 na raiz.** O limite de batimento do worker e de 90 segundos.
A maquina passou **31 minutos** sem dar sinal e o Service nao acusou nada — porque o relogio
unbiased nao conta o tempo dormido. Antes, isto daria worker morto, relancamento e alarme falso.

O log do Service pula direto de 14:18:01 para 14:49:16, sem uma linha de alarme no meio.

### Correcao ao que registrei no item 3

Eu escrevi *"cinco minutos e meio sem enviar depois de acordar"*. **O numero esta errado** — eu
contei a partir do preparo do lote, que aconteceu **antes** de dormir.

Medido nos dois testes, a partir do **despertar**:

```
item 3:  acordou 14:01:52  ->  lote falhou 14:03:30   =  98s
item 4:  acordou 14:49:10  ->  lote falhou 14:50:47   =  97s
```

Sempre ~97 segundos. Nao e travamento: e o **timeout do cliente HTTP** derrubando a conexao que
ficou pendurada desde antes do sono. Comportamento correto, so lento.

Depois disso tudo se recompoe: 13 eventos do NoisyTech e 3 do Marcos subiram inteiros.
**Nada se perde.** O que fica e ~1,5 min de atraso no primeiro envio depois de cada despertar.

Se virar incomodo, o caminho e derrubar a conexao no `PowerMode Resume` em vez de esperar o
timeout. Baixa prioridade.

### O aviso mentiroso se repetiu

`Gap de wall-clock detectado sem PowerMode event` — de novo, com Suspend e Resume registrados
logo antes. Duas ocorrencias em dois testes. Confirma que nao foi acaso.

## Item 5 — logoff e login: **PASSA nos cinco criterios**

Logoff as 14:59:48, com a sessao do Marcos (2) logada ao lado.

| # | Criterio | Resultado |
|---|---|---|
| 1 | **uma** saida, nunca duas | uma, emitida pelo **Service** — o worker morreu antes e nao logou nada |
| 2 | horario do logoff, nao do processamento | `OcorreuEm=17:59:48.094` = instante do EOF |
| 3 | login seguinte nao acusa interrupcao | suprimido (`previousLogonId ausente`, sessao nova) |
| 4 | chega ao banco em segundos | **2,4s** — lote `af497803` subiu as 14:59:50.678 |
| 5 | nenhum worker nasce entre a queda e o logoff | `sessao 1 confirmou logoff dentro da janela de 5s apos o EOF. Nao relanca.` |

**O criterio 5 e a janela de confirmacao do A-61 funcionando no caso que a motivou.**

O recolhimento do buffer da sessao 1 saiu **com dono**:

```
Buffer orfao recolhido. Sessao=1, Usuario=NoisyTech, Eventos=2.
```

Compare com o A-63, onde saiu `Sessao=0, Usuario=(desconhecido)`. Confirma que a causa do A-63 e
**so** o nome do arquivo legado, sem numero de sessao. Com o nome novo, funciona.

### Falso positivo que eu quase reportei — dois minutos "sem captura"

O log parece mostrar um buraco feio depois do login:

```
14:59:49  ConsoleConnect Sessao=3 -> "Refusing to launch worker: session 3 does not exist"
                                  -> Worker registered for session 3, PID=0
15:01:26  watchdog: "Never pinged since launch 97s ago"
15:01:33  [ERR] Failed to launch worker for session 3
15:01:39  [ERR] Failed to launch worker for session 3
15:01:54  Worker launched. Session=3, PID=9052
```

Dois minutos e cinco segundos. Parece captura morta apos todo login.

**Nao e.** O evento `SessionLogon` da sessao 3 so chegou as **15:01:39**:

```
14:59:49  ConsoleConnect  (a TELA de login aparece)
15:01:39  SessionLogon    (o usuario terminou de digitar a senha)  <- 110s depois
15:01:54  worker de pe
```

Os dois minutos sao o **Marcos digitando a senha**. Da entrada real ate o worker de pe:
**15 segundos.**

A comparacao fecha: na troca rapida para o Marcos, `ConsoleConnect` e `SessionLogon` vieram com
4s de diferenca, e o worker subiu em 8s, na `Attempt=3` do proprio laco de retentativa.

### O que sobra disso, e e pequeno

Um login normal deixa **dois `[ERR] Failed to launch worker`** no log. Nao ha erro nenhum: a
sessao ainda nao existia porque a pessoa estava digitando a senha.

Vale rebaixar para informativo e mudar o texto. **Nao e cosmetico** nesta base: esta e a terceira
mensagem enganosa encontrada hoje, e a divida 4 de D4 registra um comentario falso que sustentou
um engano por tres meses. Log que grita erro onde nao ha erro treina todo mundo a ignorar o log.

### Empate de horario, a conferir no banco

O LOGOUT e o fechamento da ultima janela sairam com o **mesmo carimbo**,
`17:59:48.0943483+00:00`. D5 pedia a saida **antes** do fechamento da ultima janela. Empate nao e
"antes" — se o backend ordenar por horario, o desempate fica por conta dele.

Nao deu problema aqui, mas e o tipo de coisa que aparece em relatorio como janela que fechou
depois da pessoa sair.

## Item 1 — reiniciar saindo da 1.5.11: **PASSA** (sessao do console)

Reboot as 15:09:42, de volta as 15:12:11.

```
15:09:18  worker da sessao 3 morre (EOF); janela fechada
15:09:19  SessionLogoff sessao 3 -> UM LOGOUT pelo Service, com o horario do EOF
15:09:19  clean-shutdown persistido pelo Service. Session=3, LogonId=3:...:NoisyTech
15:09:22  os 3 eventos pendentes SOBEM ANTES de desligar (3,5s)
15:09:23  "SCM sinalizou desligamento. Nenhum worker sera lancado a partir daqui."
--- reboot ---
15:12:11  SESSAO_INTERROMPIDA suprimida (flag presente)
15:12:11  LOGIN emitido, LogonId novo != anterior
```

**Zero erro apos o boot.** Compare com o reboot das 11:42, saindo da 1.5.10, que acusou
`SESSAO_INTERROMPIDA` falso. **A correcao de D6 e o que faz a diferenca** — sem a atribuicao no
construtor, a linha `clean-shutdown persistido pelo Service` nao existe.

A guarda de desligamento do A-61 tambem apareceu: depois do sinal do SCM, nenhum worker nasce.

---

# A-65 — sessao do segundo usuario fica "encerrada" enquanto ainda trabalha

**Severidade: ALTA** pelo efeito em relatorio. Observado, nao deduzido.

## O que o banco viu da sessao do Marcos hoje

```
14:06:53  LOGIN
14:11:49  LOGOUT  (motivo SERVICE_STOP — teste 13)
   ...    nada
15:09:xx  reboot
```

E o que aconteceu de verdade: ele **continuou logado e trabalhando** das 14:12 as 15:09 —
**57 minutos** — com eventos de janela chegando o tempo todo, ja conferidos e enviados
(`User=Marcos`).

## Duas causas somadas

**1. O LOGOUT de `SERVICE_STOP` nao e um logout de pessoa.** Quando o Service para, o worker se
despede e emite LOGOUT. Mas o **logon do Windows continua vivo** — a pessoa nao saiu de lugar
nenhum.

**2. Na volta, o LOGIN e suprimido — corretamente.** `LOGIN suprimido — LogonId ja registrado`.
E o mesmo logon, entao nao ha login novo a registrar.

Cada metade esta certa sozinha. Juntas, **abrem um par sem fechamento**: sai um LOGOUT que nunca
recebe o LOGIN correspondente de volta.

**3. E o reboot nao fechou nada.** A sessao 2 estava logada porem desconectada. So chegou
`PipeServer: worker disconnected (EOF). Session=2` — **nenhum `SessionLogoff`, nenhum LOGOUT,
nenhuma flag nova**. `clean-shutdown-2.json` continua com o carimbo das 17:11:49Z, do teste 13.

O Windows nao mandou `SessionLogoff` da sessao desconectada antes de o Service parar. O
tratamento do A-62 depende desse evento, e para a sessao do console ele veio.

## Impacto

- Relatorio mostra o Marcos saindo as 14:11 e produzindo evento ate as 15:09.
- Qualquer soma de tempo logado erra por 57 minutos, para menos.
- Alimenta o mesmo passivo de eventos abertos (33 ociosidades, 27 janelas) que esta em decisao
  com o @Steve.
- **Escala com a frota:** todo update do Agent para o Service em cada maquina. Quem estiver com
  duas contas abertas ganha um par quebrado por update.

## Caminhos (decidir com o @Bucky)

1. **`SERVICE_STOP` nao deve emitir LOGOUT.** Nao houve saida de pessoa. Se algo precisa marcar
   a interrupcao da captura, que seja um tipo proprio — nao reaproveitar LOGOUT, que tem
   significado de negocio.
2. **Se for manter**, o retorno tem de reabrir a sessao: LOGIN suprimido so quando **nao** houve
   LOGOUT daquele logon. Espelha a regra que D2 definiu para o caminho inverso.
3. **No desligamento da maquina, fechar todas as sessoes**, nao so a do console. O Service ja
   sabe quais existem (`Found N active user sessions`); ao receber o sinal do SCM, emitir o
   fechamento de cada uma.

O caminho 1 e o mais limpo e o que menos mexe.

## Aceite

- Parar e subir o Service com duas contas logadas nao gera LOGOUT sem LOGIN correspondente.
- Reiniciar a maquina com uma conta desconectada fecha a sessao dela tambem.
- Nenhuma sessao aparece encerrada com evento posterior ao encerramento.

## Pergunta em aberto sobre a flag, honestamente marcada como raciocinio

A flag de shutdown limpo e escrita quando o **worker** para, nao quando o **logon** acaba. Um
logon sobrevive a varias paradas de worker — a do Marcos sobreviveu a uma as 14:11.

Se isso vale, entao **depois da primeira parada limpa de worker, aquele logon fica marcado como
"terminou direito" ate acabar** — e uma queda de verdade mais tarde, dentro do mesmo logon,
seria suprimida.

**Nao observei isso acontecer.** So um desligamento sujo (tirar da tomada) com uma sessao ja
marcada prova ou desmente. Nao esta na lista de 15 e nao vou provocar sem o Marcos mandar.

---

# A-63 revisado — a causa raiz e outra, e maior

O que eu escrevi de manha (nome do arquivo legado sem numero de sessao) **explicava so a primeira
ocorrencia**. As 15:21 o defeito se repetiu por um caminho completamente diferente, e agora da
para nomear a causa de verdade.

## A reproducao

```
15:20:25.429  worker da sessao 1 morre (EOF)
15:20:26.481  WTS session change: SessionLogoff, SessionId=1     <- o evento CHEGA
   ...        37 segundos
15:21:03.600  Session event: SessionLogoff, SessionId=1          <- o evento e TRATADO
15:21:03.614  LOGOUT emitido pelo Service. Session=1, Usuario=(desconhecido)
15:21:03.625  Batch preparado. Events=1 User=null
15:21:04      400 PAYLOAD_INVALIDO: windowsUsername expected string, received null
```

Em laco desde entao.

## Compare com o logoff das 14:59, que deu certo

```
14:59:49.226  WTS SessionLogoff, SessionId=1
14:59:49.261  LOGOUT emitido pelo Service. Session=1, Usuario=NoisyTech   <- 35ms depois
```

**Mesma linha de codigo, mesmo caminho. A diferenca e so o atraso.**

## A causa

O Service resolve o nome do usuario **na hora de tratar** o logoff, consultando o Windows. Em
35ms a sessao ainda esta la e ele responde. Em 37 segundos a sessao ja sumiu e a consulta volta
vazia — entao sai `Usuario=(desconhecido)`, o lote sai com `windowsUsername: null`, e o backend
recusa para sempre.

## Por que os 37 segundos

A fila de eventos de sessao ficou parada tentando lancar o worker da **sessao 2**:

```
15:21:01.394  Plans A/B failed for session 2. Falling back to Task Scheduler.
15:21:03.599  Task Scheduler launch succeeded but worker process not found for session 2
15:21:03.600  Session event: SessionLogoff, SessionId=1     <- so agora destrava
```

**O lancamento lento de uma sessao segura o tratamento do logoff de outra.** E o logoff tem prazo
de validade: passou do ponto, a informacao de quem era nao existe mais.

Numa troca de usuario — que e exatamente quando as duas coisas acontecem juntas — isso e o caso
normal, nao a excecao.

## Correcao ao que registrei de manha

O nome do arquivo legado **nao e a causa raiz**, e sim uma das entradas. A causa e:

> **O Agent monta lote com `windowsUsername` nulo em vez de recusar montar, e o backend recusa
> para sempre um lote que nunca vai mudar.**

Tres entradas conhecidas para o mesmo buraco:
1. buffer legado sem numero de sessao no nome (11:45)
2. LOGOUT do Service tratado tarde demais para resolver o usuario (15:21)
3. qualquer outra que produza usuario nulo — nao ha guarda nenhuma

## Aceite revisado

- **Resolver o usuario cedo e guardar.** O nome tem de ser capturado quando o worker conecta
  (`CONNECT received. Session=1, Pid=..., User=NoisyTech`) e ficar no registro da sessao, nao ser
  consultado no momento do logoff.
- **Nunca montar lote com `windowsUsername` nulo.** Se nao ha dono, o evento nao sobe — e isso
  precisa de **um** registro, nao de um por minuto.
- **Tratar logoff fora da fila que lanca worker.** Lancamento lento nao pode atrasar logoff.
- Mantem-se o teto de tentativas do A-64: lote recusado por conteudo sai da fila.

## Observacao de metodo

Eu tinha fechado o A-63 com a explicacao do nome do arquivo. Estava incompleta e teria mandado o
@Bucky corrigir o lugar errado — renomear arquivo legado nao impediria nada do que aconteceu as
15:21.

---

## Item 6 — desligar e ligar: **NAO EXECUTADO**

O Marcos avisou que tinha feito, mas a maquina **nao desligou**:

- `LastBootUpTime` continua **15:09:42**, do reboot anterior
- o log do Service e continuo de 15:19 a 15:22, sem parada
- so ha **uma** linha `ManagerAgent Service starting` depois das 15:09

O que aconteceu as 15:20:25 foi **logoff da sessao 1 e entrada na sessao 2**, com eventos
`RemoteConnect`/`RemoteDisconnect` no meio.

Rendeu a reproducao do A-63 acima, entao nao foi perdido. Mas o item 6 continua pendente.

## Item 6 — desligar e ligar: **PASSA**

Desligado as 15:33:21 (via `shutdown /s /t 5 /f`), ligado as 15:34:20, Service de pe as 15:36:45.

**Nota de execucao:** as duas primeiras tentativas do Marcos pelo menu Iniciar **nao desligaram**
— o Windows registrou evento 42/107 (entrou e saiu de suspensao em 1 segundo) e o Service ficou
rodando sem parar. So o comando direto desligou. O `LastBootUpTime` e o unico jeito confiavel de
confirmar: se nao mudou, nao desligou.

```
15:33:09  3 eventos do NoisyTech sobem
15:33:19  worker da sessao 3 morre (EOF); janela fechada
15:33:19  SessionLogoff sessao 3 -> UM LOGOUT pelo Service, Usuario=NoisyTech, horario do EOF
15:33:19  clean-shutdown gravado para a sessao 3, com logonId
15:33:21  mais 5 eventos do NoisyTech sobem
15:33:21  "SCM sinalizou desligamento. Nenhum worker sera lancado a partir daqui."
15:33:21  desligamento gracioso: HeartbeatWriter, UpdateChecker, Watchdog, UploadWorker param em ordem
--- maquina desliga ---
15:36:50  SESSAO_INTERROMPIDA suprimida (flag presente)
15:36:50  LOGIN emitido, logonId novo != anterior
```

Oito eventos entregues antes de apagar. Nenhum alarme falso na volta. Nenhum erro.

Desta vez o `Usuario` saiu preenchido (`NoisyTech`) porque o logoff foi tratado **23ms** depois de
chegar — confirma de novo o diagnostico do A-63 revisado.

### Escalada do A-63: o lote envenenado queima o ultimo flush

```
15:33:21.698  "Tentativa final de upload antes de parar (teto 5s)."
15:33:21.710  Batch preparado. Events=1 User=null DedupHash=93926918...
15:33:26.711  "Tentativa final estourou o teto de 5s. Eventos seguem no buffer."
```

O agent reserva **5 segundos** para uma ultima tentativa antes de morrer. Esses 5 segundos foram
**inteiros** para o lote que nunca vai ser aceito. A fila real nem chegou a ser tentada.

Aqui nao houve perda — os eventos reais ja tinham subido 100ms antes, e o resto voltou no boot
seguinte, como o proprio log promete. **Mas a janela de emergencia de todo desligamento passa a
ser consumida por um lote impossivel.**

E agora sao **dois**: `93926918` (sessao 1) e `d949eb8d` (sessao 2). **93 recusas acumuladas
hoje.** Os dois sobreviveram ao desligamento e voltaram tentando 3 segundos depois do boot.

Reforca a prioridade: **o teto de tentativas do A-64 nao e melhoria de log, e protecao do
caminho critico.**
