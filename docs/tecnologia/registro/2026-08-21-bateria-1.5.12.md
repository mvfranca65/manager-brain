# Bateria 1.5.12 na maquina (2026-08-21)

> **Executor:** Marcos (acoes) + @Bucky (verificacao) | **Maquina:** DESKTOP-VMSM6LE
> **Entrega testada:** `HANDOFF-2026-08-21-A63-A64-A65.md` + R1 do @Tony
> **Review previa:** `registro/2026-08-21-review-tony-a63-a64-a65.md` (APROVADO)
> Update automatico 1.5.11 -> 1.5.12 as 16:52. Pacote publicado pelo Marcos.
>
> **Para o @Tony revisar.** Codigo continua **nao commitado**.

---

## Linha do tempo

| Hora | O que |
|---|---|
| 16:44:55 | check de update: "no update available" — pacote ainda nao estava publicado |
| 16:46 | Marcos gera o pacote 1.5.12 (depois da ultima alteracao de codigo, 16:39) |
| 16:52:05 | update 1.5.12 encontrado; download de 310MB em **13s** |
| 16:52:20 | `UpdateApplier` inicia; Plano A (WMI) |
| 16:52:34 | GOODBYE da sessao 1 com `Reason=Update to 1.5.12 — service restarting` — **texto livre antigo, esperado** (ver R2) |
| 16:53:07 | 1.5.12 no ar. Primeiro ciclo de upload ja conta `Tentativa=1/5` |
| 16:53:36 | `autonomous-buffer-S77.db` vai para quarentena |
| 16:57:24 / 16:57:27 | os **dois** lotes envenenados sao DESCARTADOS |
| 16:59:31 | primeiro ciclo com `FailedBatches=0` — **fila destravada** |
| 17:02:58 | logoff do Marcos (item 5, variacao "logoff") |
| 17:03:36 | login de volta, sessao 2, worker 1.5.12 |

Testes na arvore: **1010 verdes** (14 + 31 + 370 + 595).

---

## PASSOU — A-64 item 1 (teto de tentativas)

O contador andou entre ciclos, que e exatamente o que nunca existiu:

```
16:53:07  lote recusado por conteudo. Session=1, Events=1, Tentativa=1/5
16:54:11  ...                                              Tentativa=2/5
16:55:15  ...                                              Tentativa=3/5
16:56:20  ...                                              Tentativa=4/5
16:57:24  [ERR] lote DESCARTADO apos 5 recusas por conteudo. Session=1, Eventos=1
16:59:31  cycle complete. Uploaded=2, FailedBatches=0, Descartados=0
```

Idem para a sessao 2, as 16:57:27. **Um `[ERR]` por lote**, nao um por ciclo.

Antes, na 1.5.11: 93 recusas em uma tarde, todas `Attempt=1`, sem nunca sair da fila. A diferenca
e o contador estar **na linha do SQLite** — a migration rodou no `events.db` que ja existia na
maquina, como o @Tony conferiu no review.

O log tambem passou a dizer a classificacao: `Status=400, ..., Permanente=true`.

## PASSOU — A-64 item 2 (quarentena do buffer ilegivel)

```
16:53:36  [ERR] Buffer orfao autonomous-buffer-S77.db ilegivel apos 3 tentativas.
          Renomeado para autonomous-buffer-S77.db.corrompido e retirado da varredura.
          NAO foi apagado: o conteudo e do colaborador e pode ser recuperavel fora do agent.
```

Nenhum aviso novo depois disso. Antes: um a cada 15s, 5.760 por dia. Os tres arquivos
(`.corrompido`, `-shm`, `-wal`) estao na pasta de dados — a sobra que o @Tony registrou no review
saiu junto com o rename.

## PASSOU — A-63 (LOGOUT com dono), item 5 do roteiro

```
17:02:58.833  worker disconnected (EOF). Session=1
17:02:58.852  Evento de janela fechado por morte do worker. FinalizadoEm=20:02:58.8375020Z
17:02:59.573  Session event: SessionLogoff, SessionId=1
17:02:59.585  LOGOUT emitido pelo Service. Usuario=NoisyTech, OcorreuEm=20:02:58.8375020Z
17:02:59.598  clean-shutdown persistido. LogonId=1:134318154040674398:NoisyTech
17:02:59.599  Worker removed from registry. Session=1
17:03:03.859  WorkerWatchdog: sessao 1 confirmou logoff dentro da janela de 5s. Nao relanca.
```

As quatro verificacoes do refinamento:

| Verificacao | Resultado |
|---|---|
| Sai **exatamente uma** saida | sim |
| Com o horario do logoff, nao o do processamento | sim — `OcorreuEm` e o EOF, 748ms antes |
| **Antes** do fechamento da ultima janela | as duas usam o **mesmo** instante do EOF; linha do tempo consistente |
| Login seguinte nao acusa interrupcao | sim — saiu LOGIN, nenhum `SESSAO_INTERROMPIDA` |
| Nenhum worker nasce entre a queda e o logoff | sim — a janela do A-61 segurou |

**Nenhum HTTP 400 em toda a janela.** Todos os ciclos com `FailedBatches=0`.

## PASSOU de brinde — A-60 (varredura de buffer orfao)

```
17:03:06  Buffer orfao recolhido. Sessao=1, Usuario=NoisyTech, Eventos=2.
```

Dois eventos que tinham ficado presos no buffer da sessao 1 durante a **propria janela do
update** (16:53), recolhidos ao completar os 10 minutos de idade minima, com o nome do dono
certo. E o cenario que motivou a varredura, acontecendo sozinho.

---

## NAO exercitado — dito com todas as letras

**1. A corrida do A-63 nao se reproduziu.** O registro foi limpo as 17:02:59.599, **depois** do
LOGOUT as .585 — a ordem boa, a mesma das 14:59 de ontem. O nome veio do `WorkerRegistry`, que e
a primeira fonte e ja funcionava antes; a fonte nova (o dono guardado no CONNECT) nao precisou
entrar. **O teste prova que nada regrediu, nao que a correcao agiu.**

O caso das 15:21 precisa de troca rapida de usuario com a outra sessao demorando a subir — foi o
lancamento travado por 37s que inverteu a ordem. Continua valendo o item 7 do roteiro.

**2. O `[ERR] Failed to launch worker` tambem nao.** Ele nasce do relancamento do
`WorkerWatchdog`, que so age apos 90s sem ping. O worker da sessao 2 subiu em 36s
(`ConsoleConnect` 17:02:59 -> `SessionLogon` 17:03:35 -> worker 17:03:35). O caminho nao rodou.
Nao apareceu, e isso nao e prova.

**3. Apareceu OUTRA mensagem enganosa, do mesmo tipo**, e nao esta na lista das tres:

```
17:03:05.940  [ERR] Task Scheduler launch succeeded but worker process not found for session 2
```

Nao houve erro: a sessao 2 ainda nao existia porque a pessoa estava digitando a senha, e o worker
subiu normalmente 30s depois. E `WorkerLauncher.cs:361`, irmao do que ja foi rebaixado.

**4. Segue sem teste em maquina:** A-65 caminho 1 (parar o Service logado), caminho 3 (desligar
com conta desconectada), e os itens 1, 6, 7, 12 e 13 do roteiro.

**5. Falta a conferencia no banco:** uma unica linha de LOGOUT em `eventos_sessao`, com nome
preenchido. Nao foi feita — sem acesso, e a senha do staging e a que esta pendente para trocar.

---

## Achado novo — proponho A-66

**O worker nao consegue gravar o `session-state-N.json` de uma sessao que trocou de dono.**

**Severidade: MEDIA.** Nao e regressao — nao tem relacao com esta rodada.

```
[ERR] Falha ao persistir session-state. Path=C:\ProgramData\ManagerAgent\data\session-state-2.json
System.UnauthorizedAccessException: Access to the path '...' is denied.
```

**Causa raiz confirmada na maquina — e a mesma do A-60:**

```
dono de session-state-2.json : DESKTOP-VMSM6LE\Marcos
dono de session-state-1.json : DESKTOP-VMSM6LE\NoisyTech
```

A pasta `data\` usa a permissao padrao do Windows: quem cria o arquivo vira dono com
FullControl, os demais ficam com leitura. O arquivo da sessao 2 foi criado pelo Marcos; agora e a
conta NoisyTech que ocupa a sessao 2, e ela **le mas nao grava**.

O A-60 resolveu isso para o buffer, trocando o arquivo unico pelo arquivo por sessao. **Nao
resolve este caso**: aqui o arquivo ja e por sessao, e o problema e o SessionId ser reciclado
**entre pessoas diferentes** — que e exatamente a hipotese que o `SessionLifecycleTracker` trata
em toda parte.

**Escopo medido:** 58 ocorrencias no log da sessao 2 de hoje, 6 no de ontem, **zero** nas sessoes
1 e 3 — bate com a hipotese, porque so a sessao 2 trocou de dono.

**Consequencia:** o estado da sessao nunca e atualizado. O log do login de hoje mostra:

```
17:03:36  LOGIN emitido. SessionId=2
          LogonIdNovo=2:134318162155274653:NoisyTech
          LogonIdAnterior=2:134318056055988866:Marcos
```

O `LogonIdAnterior` continua sendo o do Marcos e vai continuar sendo, porque a gravacao falha
sempre. Toda decisao de `SESSAO_INTERROMPIDA` daquela sessao passa a se apoiar num
`previousLogonId` congelado. Hoje o efeito foi benigno; nao ha garantia de que seja sempre.

**Nao corrigi.** E area que o refinamento mandou nao encostar (`SessaoInterrompidaDecider` e o
formato da flag), e o conserto e de ACL ou de nomenclatura de arquivo — decisao do @Tony, nao
minha. O `[ERR]` esta la desde as 00:00 de hoje e ninguem viu, o que reforca o ponto da divida 3.

---

## Estado

- Codigo: **nao commitado, nao empurrado**. 1010 testes verdes.
- 1.5.12 instalada e rodando na `DESKTOP-VMSM6LE`, fila de upload limpa.
- Retomada e comandos de conferencia: `manager-srv-agent/RETOMADA-2026-08-21-LOGOFF.md`.

## Para o @Tony decidir

1. **A-66** acima — dono, severidade e se entra nesta wave ou na proxima.
2. **Item 7 do roteiro** (troca rapida de usuario) — e o unico que exercita a corrida do A-63 de
   verdade. Vale pedir ao Marcos antes de fechar o achado?
3. **`WorkerLauncher.cs:361`** — a quarta mensagem enganosa. Junto com as tres, ou fica?
